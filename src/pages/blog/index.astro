---
import BlogLayout from '../../layouts/BlogLayout.astro';
import PostCard from '../../components/blog/PostCard.astro';
import { getPublishedPosts } from '../../lib/posts';

const posts = await getPublishedPosts();
---

<BlogLayout title="What's on my mind | Carlo Perottino" description="Thoughts on AI/ML, technology leadership, and building intelligent systems.">
  <header class="mb-12">
    <h1 class="text-4xl font-bold tracking-tight text-cream">What's on my mind</h1>
    <p class="mt-3 text-lg text-warm-gray max-w-2xl">
      Ideas, musings, and things I'm exploring in AI, tech, and beyond.
    </p>
  </header>

  {posts.length === 0 ? (
    <div class="text-center py-20">
      <p class="text-warm-dim text-lg">No posts yet.</p>
      <p class="text-warm-dim/60 text-sm mt-2">Check back soon.</p>
    </div>
  ) : (
    <div class="grid gap-6 sm:grid-cols-2">
      {posts.map((post) => (
        <PostCard post={post} />
      ))}
    </div>
  )}
</BlogLayout>
